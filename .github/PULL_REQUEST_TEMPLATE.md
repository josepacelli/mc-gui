## What changed

Fixes #

## Test plan

- [ ] `swift build` and `swift test` pass
- [ ] `./packaging/build-macos.sh` produces a working `.app`/DMG (if packaging changed)
- [ ] Tested in the running app, screenshot attached (if UI/behavior visible on screen changed)
